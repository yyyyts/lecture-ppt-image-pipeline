---
name: lecture-ppt-image-pipeline
description: 将论文、书本或其他教学材料整理成 PPT 大纲和讲课稿，生成 16:9 全套缩略图拼图、逐页幻灯片视觉稿，并汇成 PDF；制作前收集风格和听众，在大纲及拼图完成后分别等待用户确认。适用于这种分阶段的图片式 PPT 流程；普通可编辑 PPTX 请使用演示文稿工作流程。
---

# 讲课 PPT 图片生成流程

按以下顺序制作有来源依据的讲课幻灯片：收集整体风格与目标听众 → 分析材料 → 编写 PPT 大纲与讲稿 → 用户确认大纲 → 生成整套缩略图拼图 → 用户确认拼图 → 逐页生成完整视觉稿 → 汇成 PDF。以用户提供的材料、听众、时长、页数、语言和视觉参考为准。用户未指定语言时，默认使用中文编写大纲、讲稿、幻灯片和交付说明；用户明确指定其他语言时，按其要求执行。无论输出语言如何，都要准确保留原文中的专有名词、公式、引文、数字与数据关系。不得添加无依据的观点、图表数据、引文、Logo、人物或产品。

## 0. 收集制作要求

- 调用 skill 时先读取用户已说明的 PPT 整体风格和目标听众。若缺少其中任一项，明确询问缺失信息，收到答复后再制作大纲。用户可以明确委托你决定某项；此时提出具体选择并记录下来。不能把未回复视为已提供信息。
- 将确定的风格和听众写入内容计划，供大纲、拼图和单页视觉稿共同使用。

## 1. 整理内容

- 阅读用户提供的论文、书本章节或其他材料。对于长书，依据用户指定的章节，或找出与本次讲课相关的部分；不得声称分析了未读的内容。
- 记录每个重要观点的来源位置、证据、限定条件和必要定义。准确保留单位、比较对象、名称、公式及引文。若补充外部资料，记录来源网址和访问日期。
- 编写带页码的 PPT 计划，每页只承担一个明确任务，并写出该页的准确标题、页面文字、视觉方向和讲稿。讲稿说明讲授顺序、过渡方式，以及有意未放进页面的细节。页数统计应包含封面和结束页（若有）。
- 用户未指定页数时，结合材料体量和讲课时长确定页数。仅在缺失信息会显著改变成品时提问；其他情况说明合理假设并继续。
- 在图片之外保存结构化内容计划。整套视觉稿制作期间，以该计划核对准确文字、数字、引用和页面顺序，而不以缩略图中的文字为准。
- 大纲和讲稿完成后，展示每页的标题、页面要点、视觉方向及讲稿摘要，邀请用户校验。收到明确确认后才生成缩略图；若用户提出修改，修订并重新展示受影响的大纲内容，等待再次确认。未回复不算确认。

## 2. 生成整套缩略图拼图

- 设计前阅读当前可用的 PPT 生成指南。使用内置图片生成工具制作拼图，并遵循 imagegen skill 关于工具模式和图片保存的要求。
- 生成**一张** 16:9 拼图，按阅读顺序展示全部幻灯片，每页都是有明确边界的缩小版，效果类似幻灯片浏览视图。为每个缩略图标明页码。整套页面采用简洁、统一的视觉体系，同时根据讲课内容保留必要的版式变化。
- 将拼图视为版式方案。若页数过多，导致拼图中的文字难以辨认，应尽可能生成清晰的网格，并以结构化内容计划确定准确文字；不得悄悄漏页。
- 使用 [references/prompts.md](references/prompts.md) 中的拼图提示词，填入已确认的大纲、讲稿、整体风格和目标听众。对照计划检查页数、顺序和内容。若漏页、重页或错序，先修正再继续。
- 向用户展示检查合格的拼图，说明总页数、页面顺序和整体风格，邀请用户校验。若有多个候选方案，也一并展示供选择。收到用户对某个方案的明确确认后，才逐页生成视觉稿；若用户提出修改，修订拼图并重新提交确认。未回复不算确认。

## 3. 逐页生成独立视觉稿

- 开始前核对大纲与拼图都已获得用户确认。若后续修改影响已确认的大纲，重新提交大纲确认，并据此更新和重新确认拼图；若只影响拼图，重新提交拼图确认。
- 将拼图中的每一页裁成单独的参考图，同时保留完整拼图，用于核对顺序和整体一致性。不得把整张拼图直接放大后当作单页成品。
- 按页码顺序，**每次图片生成调用只生成一张完整的 16:9 幻灯片**。同时提供对应缩略图裁图和准确的文字内容，并使用 [references/prompts.md](references/prompts.md) 中的单页提示词。生成整页视觉稿，不生成单独插图，也不把多页合成一张画卷。
- 尽量还原选定拼图中的主辅色、背景、构图、插图风格、图表样式、模块位置与边距、留白、页码样式和画面密度。保留拼图中有意安排的页面差异，不虚构数据或视觉证据。
- 每生成一页就检查完整度、顺序、标题、文字、专有名词、数字、图表标注、来源标记、页码和宽高比。发现错误时编辑或重生成该页。以结构化内容计划修正文字，不照抄缩略图中的错误。
- 图片生成可能写错小字。若某页反复出现文字模糊或错误，可用图片生成工具制作干净的视觉底图，再用确定性排版方式叠加准确文字、图表和标注，同时保持拼图的视觉设计。位图页面并非可编辑 PPTX；用户询问可编辑性时，应如实说明。
- 将确认后的拼图和单页图片保存到本次任务的交付目录。单页文件按顺序命名，例如从 `slide-01.png` 到最后一页。

## 4. 汇总并检查 PDF

- 按顺序将确认后的单页图片合成横向 16:9 PDF，每张图片占一页，不裁掉页面内容。创建 PDF 时遵循 pdf skill 的制作和可视化检查流程。
- 重新打开或渲染 PDF，检查页数、顺序、页面尺寸、文字可读性、边距，以及漏页或重复页。将典型页面和所有文字密集页面与内容计划逐一核对。
- 按用户要求交付 PDF、拼图、单页图片、大纲和讲稿。若用户只要求 PDF，就交付 PDF 并说明影响使用的重要限制。不得声称图片式 PDF 包含可编辑的幻灯片对象。

## 边界

- 此流程生成幻灯片图片和 PDF。仅在用户另行要求时，才使用演示文稿工作流程制作可编辑 PPTX，并尽可能使用原生文字和图表对象。单页图片可作为后续制作可编辑 PPTX 的视觉参考。
- 原材料中的图表和 Logo 应优先使用原始素材。必须精确复现的视觉内容，应直接放置原素材，不要求图片生成工具凭空重画。
- 用户提供的模板、品牌规范和准确措辞优先于通用风格建议。用户选定的拼图决定视觉设计，结构化内容计划决定事实和文字的准确性。
